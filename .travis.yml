sudo: false
dist: trusty
language: python
cache:
    pip: true
python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"
install:
    - pip install pipenv && pipenv install --dev --system
script:
    - python manage.py test
    - make flake8
notifications:
    email: false

